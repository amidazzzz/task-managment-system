spring:
  application:
    name: Task Managment System

  datasource:
    url: "jdbc:postgresql://localhost:5432/task-managment-system-db"
    username: "postgres"
    password: "123"
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
